type: breaking

`Wolfgang.DbContextBuilder-Core` is no longer published. Versions up to 0.8.1 stay on NuGet and keep resolving; move to `Wolfgang.DbContextBuilder-Core-EF6` for EF Core 6 or `Wolfgang.DbContextBuilder-Core-EF10` for net10.0 — the types and namespaces are unchanged, so it is a package-reference swap.
